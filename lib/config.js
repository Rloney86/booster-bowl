export const SEASON = "2026";
export const CURRENT_WEEK = 1;

// Flip this to false to lock picks (ex: once games start)
export const PICKS_OPEN = true;

// Display the weekly picks deadline.
export const PICKS_DEADLINE_TEXT = "Lock picks before first kickoff this week.";
